# 통합본 검증 결과

작성: 2026-09-18 / 버전: 2.0-video

## 결과

브라우저 기능 검증 **91항목 통과**, 모델 데이터 검증 **11항목 통과**. 마지막 실행에서 자바스크립트 오류, 콘솔 오류, WebGL 오류 및 외부 네트워크 요청은 없었습니다. 아래 검증은 실제 브라우저와 파일 재로딩으로 수행했으며, 측량 정확도 검증을 뜻하지 않습니다.

## 실행 환경

Browser 플러그인이 없는 환경이므로 Playwright와 Chromium, Xvfb를 사용했습니다. 컨테이너의 file:// 접근 제한 때문에 완성된 HTML의 전체 내용을 브라우저 문서에 직접 로드했습니다. 네트워크를 오프라인으로 설정하고 내장 WebGL2로 화면을 렌더링했습니다. 화면 크기는 1600×1000, 1366×768, 390×844입니다. 컨테이너에서 지원되는 소프트웨어 그래픽 환경을 사용했고 실제 단말별 성능을 보장하지 않습니다.

## 실제로 확인한 기능

| 구분 | 결과 |
|---|---|
| 초기 화면, WebGL2, 1층 계단형 열람석 | 통과 |
| 6개 영상 시점의 카메라 이동 | 통과 |
| 영상 참고 프레임 로딩, 확인·추정 내용 표시 | 통과 |
| W/A/S/D 중 W 이동, R/F 높이 이동, 마우스 시선 변경 | 통과 |
| Esc 종료, 천장·조명 표시 | 통과 |
| 조감도 회전, 휠 확대, 오른쪽 드래그 이동, 자동 회전 | 통과 |
| B1~RF 6개 층의 분리 표시 | 통과 |
| 기존 공간 검색·선택·실내 시점 | 통과 |
| 기존·신규 가구 표시, 기존 기계장비 표시 | 통과 |
| 벽 절단 및 영상 반영 창호의 절단 | 통과 |
| 설비 5종 표시, 개별 숨기기, 일괄 숨기기, 미검증 안내 | 통과 |
| 층 분리 및 간격 변경, X/Y/Z 단면 | 통과 |
| 기존 도면과 추가 프레임 열기·확대 | 통과 |
| GLB 다운로드 파일과 생성 원본의 바이트 일치 | 통과 |
| 현재 3D 화면 PNG 저장 | 통과 |
| 작은 화면의 메뉴·시점 이동·참고 패널·보행 버튼 | 통과 |
| 데스크톱·노트북·모바일 크기의 가로 넘침 | 없음 |

A/S/D와 실제 다중 터치 제스처 등 모든 개별 입력 조합을 전수검사한 것은 아닙니다. 신뢰할 수 있는 상세 실행 목록은 `validation/browser_report.json`에 있습니다.

## 영상 대조에서 점검한 표현

| 비교 지점 | 반영 및 남은 차이 |
|---|---|
| 목재 대형 계단과 밝은 좌석띠 | 기존 잘못된 예시 방향을 바꾸고 도면 층고 기준의 계단으로 재구성. 상세 치수는 근사입니다. |
| 좁은 옆계단과 금속 손잡이 | 별도 계단 단·손잡이 메시를 구성했습니다. 손잡이 연결 세부는 단순화했습니다. |
| 리브 천장, 선형 조명, 기구 | 형태와 반복 방식을 반영했습니다. 광각 영상의 휘어짐을 실제 곡률로 복제하지 않았습니다. 조도·색온도는 실측하지 않았습니다. |
| 창가 서가·책상·붙박이 열람석 | 큰 책상, 검정 조명 바, 흰색 다리, 목재 의자와 서가 모듈을 추가했습니다. 모든 책과 가구 개수를 복제하지 않았습니다. |
| 안내데스크 | 처음 시도한 후면 통로 배치를 수정해 계단 옆 통로에 긴 흰색 카운터·작업대를 배치했습니다. |
| 계단실 | 연녹색 마감과 검정 세로 난간·목재 손잡이를 반영했습니다. 미촬영 면은 추정입니다. |
| 후반 열람공간 | 가구 외형은 반영했으나 위치가 불명확합니다. ‘위치 추정’ 표시와 원본 대조 설명을 유지합니다. |
| 전체 건물 및 기존 UI | 다른 층, 부지, 층별 보기, 단면과 설비 제어를 유지했습니다. |

인위적인 사진 생성물을 결과 검증에 쓰지 않았으며, 실제 영상 프레임과 브라우저의 실제 3D 렌더를 대조했습니다. 결과는 사진과 1:1로 같은 재질이나 조명을 재현한 모델이 아니라 시각적 공간 검토용 모델입니다.

## 데이터 검증

1층 밖의 기존 메시 **148개**의 정점 위치가 원본 모델과 일치합니다. 기존 공간 ID 66개와 층 기준 높이도 유지됩니다. 모든 정점·법선은 유한한 값이고, GLB 2.0 헤더·파일 길이·전체 메시 재로딩을 확인했습니다.

모델: 249개 병합 메시 / 243,608개 삼각형 / 66개 공간.

HTML에 내장된 GLB와 별도 제공 GLB는 동일합니다. 모델 데이터의 수량 검증은 실제 시공 수량을 검증한 의미가 아닙니다.

## 수정한 동작 문제

벽 높이 절단 때문에 천장 높이의 개념 배관까지 잘리던 문제를 분리했습니다. 벽·창호의 절단은 유지하고 계단·배관의 표시를 별도로 처리합니다. 또한 체크박스에 초점이 있는 상태에서도 Esc로 실내 시점을 종료하도록 수정했습니다.

## 검증하지 않은 범위

현장 실측, 숨은 배관·전선의 실제 위치, 시공·법정 안전 검토, 물량 산출, Safari/Firefox 및 실제 휴대기기에서의 동작은 검증하지 않았습니다. 전체화면은 컨테이너의 관리 정책으로 제한되어 정상 진입을 검증하지 못했습니다. 벽 충돌·자동 계단 보행은 구현 범위 밖입니다. 원본 전체 영상의 360도 재생 기능이 아니라 영상 장면을 참고해 만든 3D 뷰어입니다.
