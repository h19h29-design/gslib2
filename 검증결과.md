# 검증 범위

2026-09-10 제작 환경에서 확인했습니다.

## 3D 모델 자체

GLB를 다시 불러와 182개 메시와 114,464개 삼각형을 확인했습니다. 모든 꼭짓점 좌표가 유한한 수인지 검사했습니다. 지하·지상 1~4층·옥상·부지의 레이어 존재, 66개 공간 메타데이터, 네 도면 묶음에 대한 출처 기록, 2층 책누리터의 바닥 개구부 및 GLB 헤더·버전·길이를 검사했습니다. 웹 뷰어용 데이터와 GLB는 같은 모델에서 생성합니다.

## 브라우저 기능

Playwright와 Chromium의 WebGL2 환경에서 **36개 점검 항목을 통과**했습니다. 인터넷 요청을 차단한 상태로 다음을 확인했습니다.

회전·확대·이동, 6개 층의 단독 표시, 공간 검색·선택, 실내 시점 전환과 W 키 이동 및 Esc 종료, 가구·기계설비 표시 전환, 벽 높이 절단, 원도면 이미지 표시, 부지 포함 외관, 옥상 숨기기, 층 분리, 단면 방향·위치 변경, GLB 다운로드와 PNG 화면 저장, 도움말, 모바일 층 선택·설정 메뉴를 점검했습니다. JavaScript 실행 오류와 WebGL 오류가 없었고 외부 HTTP/HTTPS 요청이 없었습니다.

검증 화면 크기는 1600×1000, 1366×768, 390×844입니다. 가로 넘침 여부를 검사하고 내부·외관·층 분리·단면·실내 시점 화면을 캡처해 확인했습니다.

## 검증 방식의 제한

제작 환경의 관리 정책은 `file://` 주소 탐색을 제한합니다. 따라서 파일 내용을 그대로 브라우저 문서에 로드하고, Xvfb 가상 디스플레이와 소프트웨어 그래픽 환경에서 실제 코드·3D를 실행했습니다. 사용자의 PC에서 파일 더블클릭, Safari, Edge 및 실제 모바일 기기까지 직접 시험한 것은 아닙니다. 전체화면 API도 제작 환경 정책에 제한이 있어 사용자 기기에서의 전체화면 전환까지 검증했다고 주장하지 않습니다.

이 검증은 **뷰어의 작동 여부**를 확인한 것이며 도면 전체에 대한 치수 대조, 실측 정확도, 건축·구조·피난·설비 안전 검증을 의미하지 않습니다.
